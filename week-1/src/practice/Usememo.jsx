import React, { useMemo, useState } from 'react'

const Usememo = () => {
  const [count, setCount] = useState(0)

  const total = useMemo(()=>{
    return 
  })

  return (
    <button></button>
  )
}

export default Usememo